model:
  provider: openrouter
  default: google/gemini-2.5-flash

vision:
  provider: openrouter
  model: google/gemini-3.1-flash-lite-preview

agent:
  max_turns: 10

platform_toolsets:
  api_server:
    - web
    - file
    - vision

stt:
  enabled: false
